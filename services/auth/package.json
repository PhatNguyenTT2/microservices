{
  "name": "@posmart/auth-service",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "start": "node src/index.js",
    "dev": "node --watch src/index.js",
    "test": "jest --verbose --forceExit",
    "test:unit": "jest --verbose --forceExit --testPathPattern=unit",
    "test:integration": "jest --verbose --forceExit --testPathPattern=integration"
  },
  "dependencies": {
    "bcrypt": "^5.1.1",
    "cors": "^2.8.5",
    "express": "^4.21.0",
    "express-rate-limit": "^8.3.1",
    "helmet": "^8.0.0",
    "pino-pretty": "^11.3.0"
  },
  "devDependencies": {
    "jest": "^30.3.0",
    "supertest": "^7.2.2"
  }
}
